import styled from "styled-components";

const HeaderBlock = styled.div`
    width: 100%;
`


export const Header = () => {
    return(
        <HeaderBlock>
            
        </HeaderBlock>
    )
}